mixin radio(data)
    +b.radio&attributes(attributes)
        each item, index in data
            - var checked = index === 0 ? true : false
            +e.wrapper
                +e('input')(type="radio", id=item.id, name=item.name, checked=checked).field
                +e('label')(for=item.id).label #{item.text}